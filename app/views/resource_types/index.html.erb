<div class="container bg-white p-4">

    <h1>Resource types</h1>

    <% @resource_types.each do |resource_type| %>
        <span class="fs-5 me-3"><%= link_to resource_type.name, resource_type %></span>
    <% end %>
</div>